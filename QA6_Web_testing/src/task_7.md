# Общая информация
## Название сайта: ТВОЕ (интернет-магазин одежды)

### Адрес сайта: https://tvoe.ru

### Дата валидации: 18.11.2024

## Статистика валидации
### Общее количество ошибок: 85+

### Количество предупреждений: 15+

### Критичных ошибок: 25+

### БАГ-РЕПОРТ: Критические ошибки валидации HTML на сайте tvoe.ru

| Приоритет | ID   | Заголовок       | Описание    | Шаги для воспроизведения    | Ожидаемый результат     | Фактический результат        |
| :--------: | :---: | :----------------------------------------------------------------------------------------------------:| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------: | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------: | :------------------------------------------------------------------------------------------------------: | :----------------------------------------------------------------------------------------------------------: |
| **Высокий**  | **H1** | Невалидная структура документа: элемент `<style>` внутри `<body>`                                     | Элемент `<style>` обнаружен как прямой потомок `<body>`, что нарушает спецификацию HTML. Стили должны находиться внутри `<head>`.                                                                      | 1. Открыть инструменты разработчика (F12). <br> 2. Проверить структуру DOM. <br> 3. Найти элемент `<style>` внутри `<body>`.                                                   | Все элементы `<style>` расположены внутри секции `<head>` документа.                                    | Элементы `<style>` находятся внутри `<body>`, что приводит к ошибке валидации.                               |
| **Высокий**  | **H2** | Метатег viewport ограничивает возможность масштабирования                                             | Атрибут `user-scalable=no` в метатеге viewport ограничивает возможность пользователя масштабировать страницу, что нарушает принципы доступности (WCAG).                                                 | 1. Открыть исходный код страницы. <br> 2. Найти метатег viewport. <br> 3. Проверить наличие параметров `user-scalable=no`, `maximum-scale=1.0`.                              | Пользователи могут свободно масштабировать страницу для удобства чтения.                                | Функция масштабирования на мобильных устройствах заблокирована.                                              |
| **Высокий**  | **H3** | Отсутствуют атрибуты `alt` у изображений в навигационном меню                                         | Изображения в выпадающих меню (напр., категории "Новый год", "Одежда") не имеют текстовых альтернатив (`alt`). Это делает контент недоступным для пользователей скринридеров и нарушает WCAG.           | 1. Перейти на главную страницу. <br> 2. Навести курсор на пункт меню "Женщинам". <br> 3. Открыть инструменты разработчика и проверить теги `<img>` в открывшемся меню.        | У всех информативных изображений есть описательные атрибуты `alt`.                                      | У изображений в меню отсутствуют атрибуты `alt` (`<img src="...jpg" ... />`).                                |
| **Высокий**  | **H4** | Некорректное использование атрибута `defer` в тегах `<script>`                                        | Атрибуту `defer` присвоено значение `"true"`, в то время как корректно использование булевого атрибута (`defer` без значения). Это может привести к непредсказуемому поведению при загрузке скриптов. | 1. Просмотреть исходный код страницы. <br> 2. Найти теги `<script>` с атрибутом `defer="true"`.                                                                               | Скрипты используют булевы атрибуты корректно: `defer`, а не `defer="true"`.                             | Скрипты загружаются с некорректным атрибутом: `<script ... defer="true"></script>`.                          |
| **Средний** | **M1** | Элементы `<link>` для предзагрузки шрифтов не имеют обязательного атрибута `rel="preload"`            | Многочисленные элементы `<link>` для загрузки шрифтов (например, HelveticaNeueCyr) используют атрибут `as="font"`, но не имеют атрибута `rel="preload"`, что делает их невалидными и неэффективными. | 1. Просмотреть исходный код. <br> 2. Найти элементы `<link>` с атрибутом `as="font"`. <br> 3. Проверить наличие атрибута `rel`.                                               | Элементы `<link>` для предзагрузки шрифтов имеют корректную структуру: `rel="preload" as="font"`.       | Элементы `<link>` имеют структуру: `<link href="...font.woff" as="font">` (отсутствует `rel`).               |
| **Средний** | **M2** | Избыточные атрибуты `type` у тегов `<script>`                                                         | Для скриптов JavaScript указаны избыточные атрибуты `type="text/javascript"` и `type="application/javascript"`. В HTML5 атрибут `type` не является обязательным для JS.                                | 1. Просмотреть исходный код страницы. <br> 2. Найти все теги `<script>`. <br> 3. Проверить наличие атрибутов `type`.                                                          | Для скриптов JavaScript атрибут `type` не указан, если это не необходимо.                               | Скрипты загружаются с избыточными атрибутами: `<script type="text/javascript">...`.                          |
| **Средний** | **M3** | Элементы `<link>` и `<meta>` размещены внутри `<body>`                                                | Элементы `<link>` (для иконок, стилей) и `<meta>` (с описанием, для смартбаннера) размещены внутри тела документа, хотя должны находиться в `<head>`.                                                  | 1. Открыть инструменты разработчика. <br> 2. Проверить дерево DOM. <br> 3. Найти элементы `<link>` и `<meta>` после открывающего тега `<body>`.                               | Все мета-данные и ссылки на ресурсы расположены внутри секции `<head>`.                                 | Элементы `<link>` и `<meta>` находятся внутри `<body>`, что является нарушением структуры документа.         |
| **Средний** | **M4** | Отсутствует обязательный атрибут `offset` у элементов `<stop>` в SVG                                  | Градиенты в SVG-графике содержат элементы `<stop>` без обязательного атрибута `offset`, что делает SVG невалидным.                                                                                    | 1. Найти на странице SVG-графику (например, в логотипе или иконках). <br> 2. Проверить исходный код SVG. <br> 3. Найти элементы `<stop>`.                                     | У всех элементов `<stop>` в SVG градиентах указан атрибут `offset`.                                     | Элементы `<stop>` используются без атрибута `offset` (`<stop stop-color="#00A0FF"/>`).                       |
| **Низкий**  | **L1** | Лишние завершающие косые черты в void-элементах                                                       | В void-элементах (таких как `<img>` и `<link>`) используются лишние завершающие косые черты (`/`), что не имеет эффекта в HTML5.                                                                      | 1. Просмотреть исходный код. <br> 2. Найти void-элементы, например: `<img ... />`, `<link ... />`.                                                                            | Void-элементы записаны без лишней закрывающей косой черты: `<img ...>`, `<link ...>`.                   | Void-элементы записаны с лишним слэшем: `<img ... />`.                                                       |
| **Низкий**  | **L2** | Использование нестандартных атрибутов фреймворка (Vue.js) в разметке                                  | В разметке присутствуют атрибуты, специфичные для Vue.js (например, `v-cloak`, `@click`, `:class`), которые не сериализуются как валидный HTML/XML. Это информационная ошибка.                        | 1. Открыть инструменты разработчика. <br> 2. Осмотреть элементы в DOM, особенно в корневом компоненте `#app`.                                                                 | Код шаблонов Vue компилируется в валидный HTML.                                                         | В скомпилированном DOM присутствуют несериализуемые атрибуты Vue.js.                                         |